#include <stdio.h>

int main () {
    int pass,plane,i=0;
    int seat[1000];

    for(i=0; i<100; i++) seat[i]=0;

    scanf("%d %d", &pass, &plane);

    for( i=0 ; i < plane ; i++) scanf("%d", &seat[i]);

    // min earning
    for()

    return 0;
}
